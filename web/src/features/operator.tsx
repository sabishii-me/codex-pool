import { PageFrame, StatusBadge } from "../components/ui";

export function OperatorPage({ title, description, rows, empty }: { title: string; description: string; rows: Array<{ title: string; meta: string; state: string; detail: string }>; empty?: string }) { return <PageFrame kicker="Operations" title={title} description={description}><section className="operator-list">{rows.length ? rows.map(row => <article className="model-row-new" key={row.title}><div><b>{row.title}</b><small>{row.meta}</small></div><StatusBadge tone={row.state === "healthy" ? "success" : "warning"}>{row.state}</StatusBadge><span>{row.detail}</span><button className="secondary-button">Inspect</button></article>) : <div className="empty-state"><b>{empty}</b><p>No records are currently available from the gateway data contract.</p></div>}</section></PageFrame>; }
